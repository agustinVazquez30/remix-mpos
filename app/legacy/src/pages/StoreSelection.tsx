import { Centered } from "~/legacy/src/layouts";
import { StoreSelectionLoad } from "~/legacy/src/modules";

export const StoreSelection = () => {
  return (
    <Centered>
      <StoreSelectionLoad />
    </Centered>
  );
};
